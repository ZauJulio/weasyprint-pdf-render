repos:
  - repo: local
    hooks:
      - id: run-checks
        name: Run project checks (lint, format, type, tests)
        entry: uv run task check
        language: system
        stages: [pre-push]
        pass_filenames: false
